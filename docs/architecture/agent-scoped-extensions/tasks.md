# Tasks

1. Extend shared Argos agent config types and schemas with extension allowlists.
2. Merge the new fields in agent repository config resolution.
3. Add runtime policy resolution and tool filtering in the agent runtime presenter.
4. Thread the policy through tool presenter and MCP presenter access checks.
5. Add a React settings section for per-agent extension allowlists.
6. Add or update tests for config persistence, runtime filtering, and settings save behavior.
7. Run formatting, linting, typecheck, and the relevant test suite.

