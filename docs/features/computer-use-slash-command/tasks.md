# Tasks: computer-use-slash-command

- [x] Add CUA plugin status fetch + `/computer-use` slash item.
- [x] Insert status-aware guidance on selection (enabled / disabled / broken).
- [x] Runtime-ready selection now **activates the shipped `computer-use` skill** instead
      of pasting text — the composer shows the existing skill chip (removable), and the
      full guidance reaches the agent through the skill system.
- [x] lint + typecheck.
