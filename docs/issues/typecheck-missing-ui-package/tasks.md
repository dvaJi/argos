# Tasks: typecheck-missing-ui-package

- [x] Root `typecheck` + `typecheck:web` include `@argos/ui`.
- [x] `AGENTS.md` documents the coverage.
- [x] Verified: both tasks run; injected UI type error fails the root gate.
