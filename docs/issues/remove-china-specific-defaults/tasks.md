# Tasks

- [x] Create SDD artifacts.
- [x] Remove China-specific provider defaults and install support.
- [x] Remove Chinese locale/search alias defaults.
- [x] List agent registry candidates for user decision.
- [x] Run validation (`bun run format` and `bun run lint` passed; `bun run i18n` is unavailable because no root script exists).
- [x] Remove standalone provider DB catalog module and use provider metadata helpers from providerDeeplink.ts.
- [x] Remove duplicate desktop default provider catalog and use @argos/backend-core as the single source.

