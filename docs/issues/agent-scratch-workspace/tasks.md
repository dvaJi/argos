# Tasks: agent-scratch-workspace

- [x] Fallback cwd: `process.cwd()` → `<dataDir>/agent-workspace` (created on demand).
- [x] Auto-trust the Argos-owned scratch workspace.
- [x] System prompt instructs the agent to keep scratch files in the workspace.
- [x] lint + typecheck + daemon suites.
- [ ] Manual: new chat without project → scripts land in agent-workspace.
