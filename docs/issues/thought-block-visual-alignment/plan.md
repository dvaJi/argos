# Plan

## Approach

Update `MessageBlockThink.tsx` only.

## Changes

- Reuse the existing icon style pattern used across message cards.
- Tighten header spacing and text styling.
- Give the expanded body a clearer separation similar to tool-call details.

## Validation

- Run `bun run format`.
- Run `bun run lint`.
