# Tasks: Usage dashboard cost-quality shares

- [x] Contract + daemon aggregation + view rendering (see plan).
- [x] Daemon `bun test` 351 pass (incl. new cost-quality assertions), UI/desktop/daemon
      typecheck, lint, format.
- [ ] Optional later: fetch-time validation negative test (simulate schema-drifted upstream).
