# Plan

1. Reproduce the diagnostic using the checked-in desktop node config.
2. Disable composite-project enforcement in that config, matching the existing type-check command and workspace packages.
3. Verify the config directly and through repository type-check, formatting, and linting.
