# Plan

1. Initialize argument rows and folder entries directly from the incoming configuration.
2. Remove the effects that synchronize an intermediate argument string in both directions.
3. Hydrate both argument representations explicitly when importing JSON.
4. Add a regression test that renders edit mode with multiple arguments and edits/saves them.
5. Run focused tests, type checking, React Doctor, formatting, and linting.
