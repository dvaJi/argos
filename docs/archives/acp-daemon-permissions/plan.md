# Plan

1. Track pending ACP permission requests in the daemon execution adapter.
2. Map ACP permission options to the existing full-access and overlay decisions.
3. Emit an action block for default-mode requests and resolve it through the existing interaction route.
4. Cover full-access, user approval, and cancellation behavior with focused tests.
5. Coalesce adjacent reasoning chunks before publishing stream snapshots.
