# Plan

1. Add shared route contracts for Argos agent create/update/delete.
2. Implement the route handling in the desktop and backend-core config route handlers.
3. Map the legacy presenter calls in the web bridge to the new routes and adapt the output shapes back to the legacy presenter API.
4. Validate the settings page flow in web mode and run formatting, linting, and type checking.

