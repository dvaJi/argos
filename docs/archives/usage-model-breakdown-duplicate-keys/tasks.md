# Tasks: usage-model-breakdown-duplicate-keys

- [x] Trace duplicate keys to daemon grouping `(providerId, modelId)` with `id`/`label`
      set to `modelId` only.
- [x] Fix client key to `${providerId}:${id}`.
- [x] Lint + typecheck.
