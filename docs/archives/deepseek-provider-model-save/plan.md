# Plan

1. Add the missing model routes to `apps/desktop/src/preload/webBridge.ts`.
2. Make the browser bridge return the same shapes as the legacy IPC path.
3. Validate the provider settings path with format, lint, and typecheck.
