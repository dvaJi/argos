# Plan

1. Unwrap the daemon `config.listAgents` response in `apps/desktop/src/preload/webBridge.ts`.
2. Keep the legacy presenter contract unchanged for renderer code.
3. Validate the settings page path in typecheck and lint.
