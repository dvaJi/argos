# Plan

1. Filter the status overview to detected sources containing skills.
2. Replace ambiguous copy and icon-only controls with explicit source/import language.
3. Preserve the selected tool ID and skill names in `SkillsSettings`.
4. Initialize `SkillSyncDialog` and `ImportWizard` from that selection.
5. Add renderer coverage for filtering, explanation, and import handoff.
6. Run focused tests, UI typecheck, React Doctor, formatting, and linting.
