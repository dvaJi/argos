# Tasks — Composer model picker ACP fallback

- [x] 1. Add `usePreSessionAgentType` composable.
- [x] 2. Fix `ComposerModelPicker` no-selection branch.
- [x] 3. Align `ComposerFooterBar` + `ComposerModePicker`.
- [x] 4. Validate (typecheck / lint / format).
