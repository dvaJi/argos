# Plan

1. Replace the Vite child runtime in `scripts/dev.mjs` with Node.js.
2. Add concise lifecycle messages around UI readiness and desktop startup.
3. Verify the launcher source and run repository formatting and lint checks.
