# Plan: Web and VPS Positioning

1. Audit the landing hero/features and `/docs` handbook against the daemon
   transport, authentication, and headless route boundaries.
2. Add a concise web-workspace message and documentation call to action to the
   landing without changing its layout system.
3. Extend the handbook with web/VPS setup, capability boundaries, and security
   caveats; repair stale shared-token wording in the systemd deployment guide.
4. Run formatting, lint, and the landing typecheck/build validation.

## Compatibility

The change is copy and documentation only. Existing URLs, installer commands,
and desktop positioning remain available.
