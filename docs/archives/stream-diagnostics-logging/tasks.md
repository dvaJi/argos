# Tasks: Streaming diagnostics logging

- [x] Compact delta/timing `stream.updated` log in `messageIpc` (preview → `console.debug`).
- [x] WebSocketBridge connect/close/reconnect logs + route-attributed timeout message.
- [x] Daemon WS route dispatch slow/failed logging.
- [x] Pi turn lifecycle + slow-tool (>5s) logging.
- [x] `bun run typecheck` (ui/daemon/desktop), daemon `bun test` (344 pass), desktop bridge/preload
      tests (30 pass), `bun run lint`.
