{
  "name": "@argos/daemon",
  "version": "1.0.6-beta.2",
  "type": "module",
  "bin": {
    "argos-daemon": "./src/index.ts"
  },
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "bun build --compile src/index.ts --outfile dist/argos-daemon",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@argos/backend-core": "workspace:*",
    "@argos/shared-contracts": "workspace:*",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "@types/bun": "^1.2.0"
  }
}
